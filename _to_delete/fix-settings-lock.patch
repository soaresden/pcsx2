--- a/pcsx2/SIO/Memcard/MemoryCardFile.h
+++ b/pcsx2/SIO/Memcard/MemoryCardFile.h
@@ -71,5 +71,9 @@
 std::string FileMcd_FindCardForSerial(const std::string_view serial);
 
 /// Same as FileMcd_FindCardForSerial(), but creates the card when it does not exist yet. The new
-/// card is named after the serial, the GameDB title (falling back to fallback_title) and the region.
-std::string FileMcd_GetCardForSerial(const std::string_view serial, const std::string_view fallback_title);
\ No newline at end of file
+/// card is named after the serial, the GameDB title (falling back to fallback_title) and the region,
+/// with the specified extension. When template_card names an existing card, it is copied instead of
+/// writing a blank card, which allows new cards to start out already formatted.
+/// Note: this must not query the settings itself, it is called while the settings lock is held.
+std::string FileMcd_GetCardForSerial(const std::string_view serial, const std::string_view fallback_title,
+	const std::string_view extension, const std::string_view template_card);
\ No newline at end of file
--- a/pcsx2/SIO/Memcard/MemoryCardFile.cpp
+++ b/pcsx2/SIO/Memcard/MemoryCardFile.cpp
@@ -1225,9 +1225,8 @@
 
 	// Seeds a freshly created card from the template card configured in
 	// [MemoryCards]/PerGameCardsTemplate, so that new cards can start out already formatted.
-	bool PerGameMcd_CreateFromTemplate(const std::string& full_path)
+	bool PerGameMcd_CreateFromTemplate(const std::string& full_path, const std::string_view template_name)
 	{
-		const std::string template_name = Host::GetStringSettingValue("MemoryCards", "PerGameCardsTemplate", "");
 		if (template_name.empty())
 			return false;
 
@@ -1289,7 +1288,8 @@
 	return best;
 }
 
-std::string FileMcd_GetCardForSerial(const std::string_view serial, const std::string_view fallback_title)
+std::string FileMcd_GetCardForSerial(const std::string_view serial, const std::string_view fallback_title,
+	const std::string_view extension_setting, const std::string_view template_card)
 {
 	const std::string normalized_serial(PerGameMcd_NormalizeSerial(serial));
 	if (normalized_serial.empty())
@@ -1305,8 +1305,8 @@
 		title = game->name;
 
 	const std::string_view region(PerGameMcd_GetRegionName(normalized_serial));
-	std::string extension(Host::GetStringSettingValue("MemoryCards", "PerGameCardsExtension", ".bin"));
-	if (extension.empty() || extension.front() != '.')
+	std::string extension(extension_setting.empty() ? std::string_view(".bin") : extension_setting);
+	if (extension.front() != '.')
 		extension.insert(extension.begin(), '.');
 
 	std::string name(serial);
@@ -1323,7 +1323,7 @@
 	name += extension;
 
 	const std::string full_path(Path::Combine(EmuFolders::MemoryCards, name));
-	if (FileSystem::GetPathFileSize(full_path.c_str()) <= 0 && !PerGameMcd_CreateFromTemplate(full_path))
+	if (FileSystem::GetPathFileSize(full_path.c_str()) <= 0 && !PerGameMcd_CreateFromTemplate(full_path, template_card))
 	{
 		// Cards ending in .bin/.mc2 are stored without ECC data (the format used by OPL and by real
 		// hardware dumps); everything else gets PCSX2's native ECC layout.
--- a/pcsx2/VMManager.cpp
+++ b/pcsx2/VMManager.cpp
@@ -700,8 +700,13 @@
 
 	if (s_per_game_memcard_serial != serial)
 	{
+		// NOTE: We're called with the settings lock held, so everything the resolver needs has to be
+		// read from si here, rather than through the Host::Get*SettingValue() helpers.
+		const std::string extension(si.GetStringValue("MemoryCards", "PerGameCardsExtension", ".bin"));
+		const std::string template_card(si.GetStringValue("MemoryCards", "PerGameCardsTemplate", ""));
+
 		s_per_game_memcard_serial = serial;
-		s_per_game_memcard_name = FileMcd_GetCardForSerial(serial, title);
+		s_per_game_memcard_name = FileMcd_GetCardForSerial(serial, title, extension, template_card);
 	}
 
 	if (s_per_game_memcard_name.empty())
